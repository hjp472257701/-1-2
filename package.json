{
  "name": "cloude-code",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "start": "npm --prefix server run start",
    "build": "npm ci --prefix web && npm run build --prefix web && npm ci --prefix server",
    "ci": "npm --prefix web ci && npm --prefix web run build",
    "publish:github": "bash scripts/publish-github.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
